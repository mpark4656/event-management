import styled from 'styled-components';
import SectionContainerWrapper from './SectionContainerWrapper';

const ConfigurationWrapper = styled(SectionContainerWrapper)`

`;

export default ConfigurationWrapper;